# Workflow Orchestrator

A distributed workflow orchestration engine with DAG-based task scheduling,
retry handling, crash recovery, and ML-driven bottleneck prediction.

## Local setup

1. `docker compose up -d` — starts Postgres (5432) and Redis (6379)
2. Backend setup instructions — TBD once scaffolded
